# 更新记录

## v2.1.8
- 元模块提示调整为仅在挂载失败时需要安装。
- 更新版本号与发布元数据。

## v2.1.7
- 安装日志补充预置证书统计。
- 更新版本号与发布元数据。

## v2.1.6
- 旧证书导入仅扫描根目录 cacerts-raw，移除废弃路径扫描。
- 更新版本号与发布元数据。

## v2.1.5
- 参照旧版 getevent 监听方式，提升音量键响应稳定性。
- 更新版本号与发布元数据。

## v2.1.4
- 安装时对 /dev/input/event* 指定读取，提升音量键识别可靠性。
- 更新版本号与发布元数据。

## v2.1.3
- 安装时补充 getevent 路径探测，确保音量键选择生效。
- 移除 system/etc/security/cacerts-raw 目录占位。
- 更新版本号与发布元数据。

## v2.1.2
- 调整音量键检测，避免终止提示并保持 10 秒等待。
- 更新版本号与发布元数据。

## v2.1.1
- 更新版本号与发布元数据。

## v2.1.0
- 旧模块导入兼容旧版 cacerts-raw 路径，恢复导入提示。
- 更新版本号与发布元数据。

## v2.0.9
- 恢复 10 秒等待提示，并更新导入超时说明。
- 更新版本号与发布元数据。

## v2.0.8
- 将 cacerts-raw 目录加入打包流程。
- 更新版本号与发布元数据。

## v2.0.7
- 将 raw 证书目录移到模块根目录，避免挂载覆盖。
- 更新版本号与发布元数据。

## v2.0.6
- 更新版本号与发布元数据。

## v2.0.5
- 缩短二次选择等待时间并区分不同提示的按键说明。
- 更新版本号与发布元数据。

## v2.0.4
- 增加音量键事件清空与二次选择等待，避免按键抬起导致的误触发。
- 更新版本号与发布元数据。

## v2.0.3
- 优先使用 getevent 处理音量键选择，避免第二次选择立即跳过。
- 更新版本号与发布元数据。

## v2.0.2
- 音量键选择流程改为基于时间循环等待，避免立即跳过。
- 更新版本号与发布元数据。

## v2.0.1
- 音量键选择流程调整为等待 10 秒，超时默认导入旧证书。
- 更新版本号与发布元数据。

## v2.0.0
- 音量键选择流程增加超时与默认跳过行为。
- 补充安装日志中的最终证书数量。
- 安装时支持根据设备架构选择内置 OpenSSL。
- README 增加旧证书导入与音量键说明。
- 更新版本号与发布元数据。

## v1.0.9
- 改进音量键检测逻辑，支持 getevent 作为回退方案。
- 更新版本号与发布元数据。

## v1.0.8
- 更新版本号与发布元数据。

## v1.0.7
- 在安装输出中补充 raw 证书处理统计与失败列表，便于排障。
- 安装时支持从旧模块导入证书，并提供音量键选择复制策略。
- 更新版本号与发布元数据。

## v1.0.6
- 修复 raw 证书在重启后重复生成导致的重复条目问题。
- 更新版本号与发布元数据。

## v1.0.5
- 增强安装日志输出，提供 raw 证书处理统计与失败列表。
- 更新模块描述为中文。
- 为 `cacerts-raw` 目录添加占位文件，确保仓库结构完整。

## v1.0.4
- 使内置 OpenSSL 可执行，加入 raw 证书处理并更新发布元数据。

## v1.0.3
- 版本升级至 v1.0.3。

## v1.0.2
- 模块版本升级至 1.0.2。

## v1.0.1
- 增加 raw PEM 证书在安装时转换为 `hash.N` 的支持，并完善元模块提示。
- 添加 update.json 与发布工作流，完善元数据与证书转换逻辑。
- 项目更名为 universal-cacert-installer，更新打包、README 与证书处理方式。
- 增加手动打包的 GitHub Action。
- 增加 README，说明证书目录与 raw PEM 转换方式。
- 增加 raw CA 证书命名支持。
